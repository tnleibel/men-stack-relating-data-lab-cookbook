<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageUser.username %>'s pantry</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= pageUser.username %>'s pantry</h1>
    <ul>
        <% pageUser.pantry.forEach((food) => { %>
            <li><%= food.name %></li>
        <% }) %>
    <ul>
</body>
</html>